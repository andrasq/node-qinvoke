{
  "name": "qinvoke",
  "version": "0.9.2",
  "description": "quick args gathering and function invocation",
  "main": "qinvoke.js",
  "author": "Andras",
  "license": "Apache 2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/andrasq/node-qinvoke"
  },
  "keywords": [
    "fast",
    "function",
    "procedure",
    "call"
  ],
  "scripts": {
    "test": "qnit -V && qnit test* || echo qnit not installed",
    "coverage": "nyc --version && nyc --reporter text --reporter lcov qnit test* || echo nyc not installed",
    "clean": "rm -rf .nyc_output coverage"
  },
  "dependencies": {
  },
  "devDependencies": {
    "qnit": "^0.12.0"
  }
}
