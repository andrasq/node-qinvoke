{
  "name": "qinvoke",
  "version": "0.11.1",
  "description": "quick args gathering and function invocation",
  "main": "qinvoke.js",
  "author": "Andras",
  "license": "Apache 2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/andrasq/node-qinvoke"
  },
  "keywords": [
    "fast",
    "function",
    "procedure",
    "call"
  ],
  "scripts": {
    "test": "qnit --version && qnit test* || echo qnit not installed, cannot unit test",
    "coverage": "nyc --version && nyc --reporter text --reporter lcov qnit test* || echo nyc not installed, cannot test coverage",
    "clean": "rm -rf .nyc_output coverage"
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
